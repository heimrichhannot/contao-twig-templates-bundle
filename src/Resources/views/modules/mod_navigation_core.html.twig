<!-- indexer::stop -->
<nav class="{{ class|default }} block"{{ cssID|default }}{% if style|default %} style="{{ style }}"{% endif %} itemscope
     itemtype="http://schema.org/SiteNavigationElement">

    {% if headline|default %}
        <{{ hl|default('h1') }}>{{ headline }}</{{ hl|default('h1') }}>
    {% endif %}

    <a href="{{ request|default ~ '#' ~ skipId|default }}" class="invisible">{{ skipNavigation|default }}</a>

    {{ items|default }}

    <a id="{{ skipId|default }}" class="invisible">&nbsp;</a>

</nav>
<!-- indexer::continue -->