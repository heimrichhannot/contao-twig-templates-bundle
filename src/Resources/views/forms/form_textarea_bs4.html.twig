{% extends '@ContaoTwigTemplates/forms/form_row_bs4.html.twig' %}

{% block field %}
    <textarea class="form-control {{ strClass }}" name="{{ strName }}" id="ctrl_{{ strId }}" rows="{{ intRows|default(3) }}" {{ attributes|default()|raw }} {{ arrConfiguration.placeholder|default|raw }}>{{ varValue|default() }}</textarea>
{% endblock %}

