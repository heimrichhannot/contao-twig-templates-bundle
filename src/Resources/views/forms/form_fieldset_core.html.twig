
{% if arrConfiguration.fsType == 'fsStart' %}

<fieldset{% if strClass|default %} class="{{ strClass }}"{% endif %}>
    {% if strLabel|default %}
        <legend>{{ strLabel }}</legend>
    {% endif %}

{% else %}

    </fieldset>

{% endif %}
