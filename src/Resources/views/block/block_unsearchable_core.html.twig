
<!-- indexer::stop -->
<div class="{{ class }} block"{{ cssID }}{% if style|default() %} style="{{ style }}"{% endif %}>
    {% block headline %}
        {% if headline|default() %}
            <{{ hl|default('h1') }}>{{ headline }}</{{ hl|default('h1') }}>
        {% endif %}
    {% endblock %}

    {% block content %}
    {% endblock %}
</div>
<!-- indexer::continue -->