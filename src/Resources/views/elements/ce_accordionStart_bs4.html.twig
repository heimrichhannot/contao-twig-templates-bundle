{% if accordion_first|default %}
<div class="accordion" id="accordion_{{ accordion_parentId }}">
    {% endif %}

    <div class="{{ class }} ce_accordion card block"{{ cssID }}{{ style|default ? ' style="' ~ style ~ '"' }}>
        <div class="card-header" id="heading_{{ id }}">
            <h3 class="mb-0">
                <button class="btn btn-link" type="button" data-toggle="collapse" data-target="#collapse_{{ id }}"{% if accordion_first|default %} aria-expanded="true"{% endif %}
                        aria-controls="collapse_{{ id }}">
                    {{ headline }}
                </button>
            </h3>
        </div>

        <div id="collapse_{{ id }}" class="collapse{% if accordion_first|default %} show{% endif %}"
             aria-labelledby="heading_{{ id }}"{% if accordion_parentId|default %} data-parent="#accordion_{{ accordion_parentId }}"{% endif %}>
            <div class="card-body">